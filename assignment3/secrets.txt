To use secrets, I created a secrets.yaml in each of the k8 directorys with the secret information and added *secrets.yaml to .gitignore.

To base64 encode the values, I ran the following command 'echo -n <value> | base64'

For db:

I added a db/k8/secrets.yaml 

apiVersion: v1
kind: Secret
metadata:
  name: mysql-info
data:
  root-password: dGhpc2lzYXRlc3R0aGluZy4=
  database: R2lmdENhcmRTaXRl

then updated db/k8/db-deployment.yaml to reference tthe secret
